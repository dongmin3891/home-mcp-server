apiVersion: apps/v1
kind: Deployment
metadata:
  name: home-mcp-server
  namespace: mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: home-mcp-server
  template:
    metadata:
      labels:
        app: home-mcp-server
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: home-mcp-server
          image: ghcr.io/dongmin3891/home-mcp-server:29b1374e0d5a61ab30e8a2618a3bb1493e4d8c79
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: PORT
              value: "3000"
            - name: HOST
              value: "0.0.0.0"
            - name: IWTC_API_BASE_URL
              value: "http://iwtc-backend.iwtc.svc.cluster.local"
            - name: IWTC_AUTOMATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: home-mcp-server-secrets
                  key: IWTC_AUTOMATION_TOKEN
                  optional: true
            - name: MCP_API_KEY
              valueFrom:
                secretKeyRef:
                  name: home-mcp-server-secrets
                  key: MCP_API_KEY
            - name: PEXELS_API_KEY
              valueFrom:
                secretKeyRef:
                  name: home-mcp-server-secrets
                  key: PEXELS_API_KEY
                  optional: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
