apiVersion: v1
kind: Service
metadata:
  name: home-mcp-server
  namespace: mcp
spec:
  selector:
    app: home-mcp-server
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
